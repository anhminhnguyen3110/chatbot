# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on vercel

# AI Gateway API Key (required for non-Vercel deployments)
# For Vercel deployments, OIDC tokens are used automatically
AI_API_KEY=

# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/storage/vercel-postgres/quickstart
POSTGRES_URL=postgresql://ai_chatbot_user:ai_chatbot_password@localhost:5432/ai_chatbot

# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=redis://redis-stack:6379

# AWS CloudWatch Logging Configuration
# Instructions: 
# 1. Create AWS IAM user with CloudWatch Logs permissions
# 2. Generate access keys and add them below
# 3. Create log group in AWS CloudWatch console or it will be auto-created
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_CLOUDWATCH_LOG_GROUP=/aws/nextjs/ai-chatbot
AWS_CLOUDWATCH_LOG_STREAM=production-logs

# Optional: Enable CloudWatch in development (default: false)
ENABLE_CLOUDWATCH_DEV=false

# Optional: Set logging level (debug, info, warn, error)
LOG_LEVEL=info